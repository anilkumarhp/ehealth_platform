[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    llm: Tests for the LLM service
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning